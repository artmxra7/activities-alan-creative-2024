var rows2 = 5; // input the number of rows

// Looping untuk menampilkan asterisks dengan nested loop
for (var i = 0; i < rows2; i++) {
  var asterisks = '';
  for (var j = 0; j < rows2; j++) {
    asterisks += '*';
  }
  console.log(asterisks);
}
